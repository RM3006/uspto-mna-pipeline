version: 2

data_tests:
  - name: assert_assignee_name_is_present
    description: >
      Data Quality Test: Validates that no record in the staging table has a NULL assignee name.
      Failure indicates a parsing error or corrupted XML source file.